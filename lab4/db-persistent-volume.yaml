apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-persistent-volume
spec:
  storageClassName: db-storage
  capacity:
    storage: 500Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/db-data"
